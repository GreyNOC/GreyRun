# GreyRun runs on the Python standard library alone, but these two packages
# are strongly recommended — without them the monitor falls back to polling
# and the responder cannot perform process-level containment.
psutil>=5.9        # process identification, suspend/terminate
watchdog>=2.1      # efficient OS-level filesystem events
